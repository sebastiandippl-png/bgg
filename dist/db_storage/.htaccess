# Deny direct access to stored data files in this directory
<FilesMatch "\.(db|sqlite|json|xml)$">
    Deny from all
</FilesMatch>

# Allow access to PHP files only through API endpoints
<FilesMatch "\.(php)$">
    Allow from all
</FilesMatch>
